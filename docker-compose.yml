services:
  qdrant:
    image: qdrant/qdrant:v1.13.4
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

  # api and frontend services are added in Phase 9 (docker/api, docker/frontend)

volumes:
  qdrant_data:
